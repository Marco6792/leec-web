import { SitePage } from "@/components/site-page";

export const metadata = {
  title: "Services — LEEC",
  description: "Services offered by the Laboratory of Electrical Engineering and Computing (LEEC).",
};

export default function ServicesPage() {
  return <SitePage slug="services" badge="Services" />;
}
